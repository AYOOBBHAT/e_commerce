import type { AlertItem } from '@/lib/alerts/types';

/** Future: deliver alert payloads to admin email inboxes. */
export async function sendAdminEmailAlert(_alert: AlertItem): Promise<void> {
  // future implementation
}
