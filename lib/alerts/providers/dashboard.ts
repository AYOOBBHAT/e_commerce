import type { AlertItem, AlertSummary } from '@/lib/alerts/types';

/** Future: persist or broadcast alerts for in-app notification feeds. */
export async function createDashboardAlert(
  _alert: AlertItem,
  _summary: AlertSummary,
): Promise<void> {
  // future implementation
}
